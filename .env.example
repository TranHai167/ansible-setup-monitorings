# AWS SES SMTP Credentials
# Tạo tại: AWS Console → SES → SMTP Settings → Create SMTP Credentials
SES_SMTP_USERNAME=
SES_SMTP_PASSWORD=

# Alert Email Config
FROM_EMAIL=cloudtrail@sts.vn
TO_EMAIL=awscloudtraillogs@sts.vn

# Target EC2 Private IP
TARGET_IP=
